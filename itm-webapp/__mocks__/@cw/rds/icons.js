const React = require('react');

const Clock = (props) => React.createElement('svg', props);
const MapIcon = (props) => React.createElement('svg', props);
const Calendar = (props) => React.createElement('svg', props);
const ActivityLog = (props) => React.createElement('svg', props);

module.exports = {
  Clock,
  MapIcon,
  Calendar,
  ActivityLog,
};
